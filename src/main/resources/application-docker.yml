spring:
  datasource:
    url: jdbc:postgresql://db:5432/university_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

jwt:
  secret: ${JWT_SECRET:BagdauletAskarSecretKey2024UniversityManagementSystem}
  expiration: 86400000

logging:
  level:
    com.example.finalproject100: INFO
